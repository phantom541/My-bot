module.exports = {
  name: 'challenge-beast',
  description: 'Fight the active Colossal Beast.',
  async execute(context) {
    const { reply } = context;
    // This is a placeholder for the challenge-beast logic.
    await reply('You are now challenging the Colossal Beast!');
  },
};
